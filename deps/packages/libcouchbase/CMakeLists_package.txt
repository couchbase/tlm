if (CMAKE_BUILD_TYPE STREQUAL "Debug")
    if (WIN32)
        FILE (COPY bin/Debug/ DESTINATION "${CMAKE_INSTALL_PREFIX}/bin")
    else (WIN32)
        FILE (COPY lib/Debug/ DESTINATION "${CMAKE_INSTALL_PREFIX}/lib")
    endif (WIN32)
else (CMAKE_BUILD_TYPE STREQUAL "Debug")
    if (WIN32)
        FILE (COPY bin/Release/ DESTINATION "${CMAKE_INSTALL_PREFIX}/bin")
    else (WIN32)
        FILE (COPY lib/Release/ DESTINATION "${CMAKE_INSTALL_PREFIX}/lib")
    endif (WIN32)
endif (CMAKE_BUILD_TYPE STREQUAL "Debug")
