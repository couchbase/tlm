IF (WIN32)
    # Put correct files in here
    # FILE (COPY lib/v8.dll DESTINATION "${CMAKE_INSTALL_PREFIX}/bin")
ELSE (WIN32)
    FILE (COPY bin lib DESTINATION "${CMAKE_INSTALL_PREFIX}")
ENDIF (WIN32)
